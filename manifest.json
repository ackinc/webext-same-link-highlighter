{
  "manifest_version": 3,
  "name": "Same Link Highlighter",
  "version": "1.0",
  "description": "Highlights all links with the same destination when hovering over a link. Helpful when reading documentation with lots of internal links.",
  "permissions": ["activeTab", "scripting"],
  "action": {},
  "background": {
    "service_worker": "background.js"
  }
}
